<div class="row">
    <div class="col-md-12">
        <form class="form-horizontal" id="basicForm">
            <div class="form-group">
                 <div class="col-sm-12">
                    <select id="loja" class="width100p" data-placeholder="Selecione uma loja" tabindex="-1">
                        <option value="">Todas</option>
                        {% for loja in lojas %}
                        <option value="{{ loja.getId }}">{{ loja.getNOme }}</option>
                        {% endfor %}
                    </select>
                 </div>
            </div>
            <div class="form-group">
                <label class="col-sm-4 control-label">Native Radio</label>
                <div class="col-sm-8">
                    <div class="radio"><label><input name="tipo" type="radio" checked=""> Clientes em atraso</label></div>
                    <div class="radio"><label><input name="tipo" type="radio"> Clientes com acordo em andamento</label></div>
                    <div class="radio"><label><input name="tipo" type="radio"> Clientes enviados para cobrança judicial</label></div>
                </div>
            </div>
        </form>
    </div>
</div>